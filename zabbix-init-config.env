# Параметры для автоматической настройки Zabbix (скрипт zabbix-init-config.py)
# Скопируйте в zabbix-init-config.local.env и при необходимости измените.
# Загрузка: source zabbix-init-config.local.env  или  export $(grep -v '^#' zabbix-init-config.local.env | xargs)

# URL веб-интерфейса Zabbix (доступный с машины, где запускается скрипт). Замените на свой IP при удалённом запуске.
ZABBIX_URL=http://127.0.0.1:8080

# Учётные данные (по умолчанию после установки)
ZABBIX_USER=Admin
ZABBIX_PASSWORD=zabbix

# Имя хоста в Zabbix (должно совпадать с ZBX_HOSTNAME в docker-compose / .env агента)
ZBX_HOSTNAME=Visiology-Server

# IP агента для интерфейса хоста. Когда Zabbix в Docker, сервер опрашивает агента с хоста — укажите 172.17.0.1 (gateway docker0). Иначе — IP сервера или 127.0.0.1
ZABBIX_AGENT_IP=172.17.0.1

# Порт агента
ZABBIX_AGENT_PORT=10050

# Имя группы хостов
ZABBIX_HOST_GROUP=Visiology

# Шаблоны (имена как в Zabbix)
ZABBIX_TEMPLATE_LINUX=Linux by Zabbix agent 2
ZABBIX_TEMPLATE_DOCKER=Docker by Zabbix agent 2
