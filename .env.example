# Скопируйте в .env и при необходимости измените
POSTGRES_USER=zabbix
POSTGRES_PASSWORD=zabbix
POSTGRES_DB=zabbix

# Порт веб-интерфейса (на хосте)
ZABBIX_WEB_PORT=8080
ZABBIX_WEB_SSL_PORT=8443
ZABBIX_SERVER_PORT=10051
ZABBIX_AGENT2_PORT=10050

# Адреса, с которых агент принимает подключения (Server). Нужны 127.0.0.1 и подсеть Docker,
# чтобы сервер из контейнера мог опрашивать агент на хосте. По умолчанию: 127.0.0.1,172.16.0.0/12
# ZBX_SERVER_HOST=127.0.0.1,172.16.0.0/12

# Образ агента: по умолчанию zabbix-agent2-with-curl (собирается установщиком из Dockerfile.agent2).
# Если образ не собирали — укажите стандартный: ZABBIX_AGENT2_IMAGE=zabbix/zabbix-agent2:alpine-7.4-latest
# ZABBIX_AGENT2_IMAGE=zabbix-agent2-with-curl

# Имя хоста в Zabbix и метаданные для авторегистрации
ZBX_HOSTNAME=Visiology-Server
ZBX_HOSTMETADATA=visiology

# Часовой пояс веб-интерфейса
TZ=Europe/Moscow
