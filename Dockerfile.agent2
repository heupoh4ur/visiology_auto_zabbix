# Образ Zabbix Agent 2 с curl, jq и docker-cli для виджетов дашборда (docker service ls, docker ps).
# Сборка: docker build -f Dockerfile.agent2 -t zabbix-agent2-with-curl .
FROM zabbix/zabbix-agent2:alpine-7.4-latest
USER root
# docker-cli — для UserParameter (виджеты контейнеров). Агент под root — доступ к /var/run/docker.sock.
RUN apk add --no-cache curl jq docker-cli \
    && which docker && docker --version
# Оставляем USER root: иначе агент (zabbix) не имеет доступа к docker.sock на хосте
