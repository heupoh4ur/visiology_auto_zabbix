# Переопределение ServerActive: образ подставляет ZBX_SERVER_HOST и в Server, и в ServerActive.
# CIDR (172.16.0.0/12) допустим только в Server; для ServerActive нужен один хост.
ServerActive=127.0.0.1
